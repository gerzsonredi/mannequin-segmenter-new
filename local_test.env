# Local Development Environment Variables
# Copy this content to .env for local testing

# AWS Configuration (mock values for local testing - S3 uploads will fail but app will start)
AWS_ACCESS_KEY_ID=mock-access-key-id
AWS_SECRET_ACCESS_KEY=mock-secret-access-key
AWS_S3_BUCKET_NAME=mock-bucket-name
AWS_S3_REGION=us-east-1

# Application Configuration
ENVIRONMENT=development
PYTHONPATH=.

# Performance Optimizations (disabled for local testing)
# PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:64,expandable_segments:True
OMP_NUM_THREADS=8
MKL_NUM_THREADS=8

# Local Testing
PORT=5001
HOST=0.0.0.0 