# Local Development Environment Variables
# Copy this content to .env for local testing

# GCP Configuration (mock values for local testing - GCS uploads will fail but app will start)
GCP_PROJECT_ID=mock-project-id
GCP_SA_KEY=mock-service-account-key-base64
GCP_BUCKET_NAME=pictures-not-public

# Application Configuration
ENVIRONMENT=development
PYTHONPATH=.

# Performance Optimizations (disabled for local testing)
# PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:64
OMP_NUM_THREADS=8
MKL_NUM_THREADS=8

# Local Testing
PORT=5001
HOST=0.0.0.0 